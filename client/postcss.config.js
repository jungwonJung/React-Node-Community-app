module.exports = {
  plugins: ["tailwindcss", "postcss-preset-env"],
};

// What is PostCSS
// convert our css to modern CSS
// postCSS using JS plugin convert CSS tool
// postCSS is not a language just one of plugins for convert to modern css
